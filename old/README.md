# old

Архив старых и лишних файлов проекта.

Здесь лежат файлы, которые не нужны для текущей сборки и деплоя GO IRL v0:

- старые zip-архивы;
- старые preview-изображения;
- сгенерированные TypeScript/Vite артефакты;
- старая заготовка `apps/telegram-miniapp` без исходников приложения.

Актуальная рабочая версия находится в корне проекта: `src`, `supabase`, `index.html`, `vite.config.ts`, `vercel.json`.
